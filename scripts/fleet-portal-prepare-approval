#!/bin/bash
set -Eeuo pipefail
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
test "$(id -u)" -eq 0 || { echo "root required" >&2; exit 77; }
exec /usr/bin/python3 /opt/agency-os/current/scripts/prepare_fleet_portal_approval.py "$@"
